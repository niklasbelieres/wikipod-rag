# Prefer `pip install -e .` (or `.[dev]`/`.[llm]`) over this file directly.

libzim>=3.6
beautifulsoup4>=4.12
pydantic>=2.6
pyyaml>=6.0
numpy>=1.26
requests>=2.31
sentence-transformers>=3.0
opensearch-py>=2.6
click>=8.1
rich>=13.7
